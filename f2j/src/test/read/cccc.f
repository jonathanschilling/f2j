c comment before program
      program cccc
c comment after program
      integer x
c  comment between var decs
      integer y
c comment after var decs
      write(*,*) 'hi'
c comment in stmts
      write(*,*) 'bye'
c comment before end
      end
c comment after end
